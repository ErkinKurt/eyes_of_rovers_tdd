name: eyes_of_rovers_tdd
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  chopper: ^4.0.1
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  injectable: ^1.4.1
  json_serializable: ^4.1.4
  mockito: ^5.0.14
  mocktail: ^0.1.4
  flutter_dotenv: ^5.0.2

dev_dependencies:
  build_runner: ^2.1.1  
  chopper_generator: ^4.0.1
  injectable_generator: ^1.4.1

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
  - .env